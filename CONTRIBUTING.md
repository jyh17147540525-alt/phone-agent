# 贡献指南

[English](CONTRIBUTING.en.md) | **简体中文**

感谢你有兴趣参与 PocketAgent。

在提交任何内容之前，请先读完本文件。**本项目的三条红线不可协商**，
违反的 PR 会被直接关闭，不另行讨论。

---

## 🚫 三条红线

这三条**不可协商，也没有解锁机制** —— 不存在「读完免责声明即可关闭」的选项。
原因见 [README 的安全边界](README.md#安全边界)：这些限制约束的主要是**我们与第三方及法律的关系**，
而不是你和自己的关系 —— 免责声明在后者有效，在前者完全无效。

### 红线一：不得涉及支付与金融操作

**禁止**提交任何针对以下页面的规则或插件：

- 支付、付款、转账、收款
- 密码输入、验证码输入、指纹/面容支付
- 银行卡、身份证、账单、充值、提现、贷款

本应用会在代码层面拦截这些页面，**绕过拦截的尝试一律视为恶意贡献**。

### 红线二：不得用于薅羊毛与作弊

**禁止**提交以下用途的规则或插件：

- 抢红包、自动签到领券、刷单、秒杀脚本
- 游戏辅助、外挂、自动挂机
- 批量注册、批量操作、爬取他人数据
- 任何以"绕过第三方应用的风控"为目的的内容

### 红线三：不得申请被禁止的能力

插件与规则中**不得**出现以下能力：

```
payment.*    key.*    crypto.*    system.*
```

这些能力在架构层面就不对外开放，申请会被自动拒绝。

---

## 可以贡献什么

### 1. 规则包（L1）—— 最推荐，门槛最低

在某个 App 的某个页面上，按特定条件执行特定动作。

**写规则的正确流程**：

1. 在真机上打开目标页面
2. 用应用内的**快照审查工具**查看节点树
3. 确认目标节点的 `vid`（资源 ID）、`text`、层级关系、`clickable` 状态
4. 写最小匹配条件（**只匹配必要的，不要贪多**）
5. 在真机上测试：改变窗口内容、返回再进入，观察是否重复触发
6. 提交 PR，说明适用场景与已知误触风险

**选择器必须包含层级关系，不能只匹配文本。**
只写 `text:广告` 会在菜单、列表、弹窗同时命中，导致一次匹配多次点击。

正确写法：
```
vid:com.example.app:id/menu > [clickable=true] text^:广告
```

### 2. 机型适配反馈

我们没有预算购买测试机。如果你在某个 ROM 上遇到权限引导路径不一致、
服务被系统杀掉、悬浮窗无法显示等问题，**请务必反馈**。

请使用 [机型适配 Issue 模板](.github/ISSUE_TEMPLATE/rom_compat.yml)，
附上：机型、ROM 版本、Android 版本、实际路径截图。

### 3. 插件（L2 / L3）

参考 [插件 API 文档](android/plugin/api/) 与 [插件模板仓库](#)（待创建）。

### 4. 文档、翻译、测试

同样欢迎。

---

## 提交规范

### 分支

```
main           可发布状态，受保护
 └── develop   集成分支
      ├── feature/<模块>-<简述>
      ├── fix/<简述>
      └── docs/<简述>
```

### 提交信息（Conventional Commits）

```
<type>(<scope>): <描述>

type:  feat | fix | refactor | perf | test | docs | build | ci | chore | security
scope: 模块名，如 action / perception / plugin-rules / display
```

示例：
```
feat(plugin-rules): 新增选择器对 desc 属性的支持
fix(display): 修复虚拟屏释放顺序导致的黑屏
security(crypto): Key 使用后强制清零，修复异常路径泄漏
```

**`security` 类型的提交必须由维护者 review。**

---

## 代码规范（适用于本体开发）

| 规则 | 说明 |
|------|------|
| 禁止 `!!` | 除非有注释说明为何不可能为 null |
| 禁止吞异常 | `catch (e: Exception) {}` 会被打回 |
| 公开 API 必须有 KDoc | 说明**为什么**，不只是**是什么** |
| 单文件不超过 500 行 | 超出则拆分 |
| 核心逻辑测试覆盖率 ≥ 70% | |

### 会被直接打回的写法

| 问题 | 原因 |
|------|------|
| 明文 Key 落盘或落日志 | 安全红线 |
| 绕过 `LogSanitizer` 直接打日志 | 同上 |
| 直接依赖 `AccessibilityService` 而非 `ActionExecutor` 抽象 | 架构约束（Android 17 收紧） |
| 新增未被 `SafetyGuard` 覆盖的动作路径 | 安全底线 |
| 截图 Bitmap 没有明确的 recycle 路径 | 内存泄漏 |
| 引入新的敏感权限 | 需维护者批准 |
| 硬编码任何形式的凭据 | 安全红线 |

---

## ⚠️ 提交前自查清单

```
□ 我没有提交任何凭据、token、密钥、签名文件
□ 我没有提交包含真实屏幕内容的快照文件
□ 我的规则不涉及支付、密码、验证码页面
□ 我的规则不涉及薅羊毛、游戏辅助
□ 我的规则没有申请被禁止的能力
□ 我的规则已用快照工具验证过选择器
□ 我的规则说明了适用场景与已知误触风险
□ 我已在真机上测试过（不是只看代码觉得对）
□ 提交信息符合 Conventional Commits 规范
```

---

## 许可证

提交即表示你同意以 [GPL-3.0](LICENSE) 授权你的贡献。

---

## 行为准则

- 对事不对人。批评代码可以，攻击人不行。
- 新人提问请耐心。你曾经也是新人。
- 不讨论与本项目无关的敏感话题。
- 发现安全漏洞请**私下**报告，不要在公开 Issue 中披露细节。
