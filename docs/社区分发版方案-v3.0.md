# 社区分发版方案 v3.0（身份定位 · 虚拟屏 · 开源治理）

> 项目：PocketAgent（手机 AI 智能体助手 · BYOK 版）
> 版本：v3.0 ｜ 日期：2026-09-21
> 前置文档：v1.0 项目方案 / M0 落地手册 / v2.0 社区分发版方案
> **本版新增三项关键约束**：① 开发者身份为在校学生、全开源、非盈利 ② 需要"虚拟屏幕"运行 Agent ③ 未来提供可推送到指定 GitHub 仓库的密钥

---

## 目录

- [0. 本轮三项约束的实质影响](#0-本轮三项约束的实质影响)
- [1. 身份定位：学生 / 开源 / 非盈利带来的合规变化](#1-身份定位学生--开源--非盈利带来的合规变化)
- [2. 虚拟屏幕：技术可行性与完整方案](#2-虚拟屏幕技术可行性与完整方案)
- [3. 执行层重构：以显示为第一维度](#3-执行层重构以显示为第一维度)
- [4. GitHub 仓库推送能力设计](#4-github-仓库推送能力设计)
- [5. 开源项目治理](#5-开源项目治理)
- [6. 修订后的模块划分](#6-修订后的模块划分)
- [7. 修订后的分阶段计划](#7-修订后的分阶段计划)
- [8. 待确认决策点（v3.0）](#8-待确认决策点v30)

---

## 0. 本轮三项约束的实质影响

| 约束 | 影响性质 | 一句话结论 |
|------|----------|-----------|
| **学生 / 开源 / 非盈利** | 合规大幅简化 | 备案与许可证压力显著下降，但**备案存在结构性死结**（见 1.3） |
| **虚拟屏幕运行 Agent** | **技术路线重排** | 存在一条 Android 10 起的硬性安全限制，第三方 App **不能**把别人的 App 启动到虚拟屏上；唯一出路是 Shizuku 通道 |
| **未来提供 GitHub 推送密钥** | 新增安全红线 | **绝不能把 token 硬编码进 APK**，否则等于把你的仓库公开送人 |

### 0.1 最重要的一个发现（先说结论）

> **Android 10 起，应用只能在自己创建的虚拟屏幕上启动自己的 Activity。**
> —— AOSP 官方文档《activity 启动政策》
>
> 这意味着：**普通的第三方应用，无法把微信、美团这类别人的 App 启动到虚拟屏幕上。**
> 虚拟屏上只能跑我们自己的 Activity。

所以"调用虚拟屏幕运行 Agent"这个需求，**只有一条路能走通：Shizuku**（拿到 shell 权限，用 `am start --display` 把目标 App 启到副屏上）。

如果 Shizuku 这条路验证失败，虚拟屏方案就不成立，必须降级为小窗模式或全屏接管。**这是 M0 的第一优先级实验（EX-16）。**

---

## 1. 身份定位：学生 / 开源 / 非盈利带来的合规变化

### 1.1 定位变化

| 维度 | v2.0 假设 | v3.0 实际 |
|------|-----------|-----------|
| 开发者 | 未明确 | **在校大一学生，个人开发者** |
| 项目性质 | 可能商业化 | **全部开源，非盈利，无任何收费** |
| 主体资质 | 可能为企业 | **个人** |
| 持续性 | 长期运营 | 可能随学业变化，需考虑交接 |

### 1.2 合规压力对比

| 合规项 | v2.0 判断 | v3.0 修订 | 变化原因 |
|--------|-----------|-----------|----------|
| **App 备案** | 义务仍在，敞口小 | **存在结构性死结**（见 1.3） | 无服务器 → 无法完成备案流程 |
| **ICP 许可证** | 不涉及 | ✅ **确定不涉及** | 非经营性、无收费 |
| **生成式 AI 备案** | 不涉及（工具定位） | ✅ **更加确定不涉及** | 开源 + 非盈利 + 无自有模型服务 |
| **个人信息保护法** | 需合规设计 | ✅ **压力极低** | 不收集数据、无账号、无服务端 |
| **开源许可证合规** | 需选择 | ⚠️ **需认真处理** | 依赖库的许可证传染性（见 5.1） |
| **学生身份的特殊性** | — | ⚠️ 需注意 | 见 1.4 |

### 1.3 备案的结构性死结（必须正面承认）

**App 备案的流程是**：通过**接入服务商**（云服务商）向省级通信管理局提交，备案信息绑定「域名 + 服务器 + 包名 + 签名」。

**矛盾点**：

```
要备案  →  需要接入服务商  →  需要国内服务器与域名
                                          ↑
                            与本项目"无自有服务端"原则直接冲突
```

**三种选择**：

| 方案 | 做法 | 代价 | 建议 |
|------|------|------|------|
| **A. 不备案，接受风险** | 保持无服务端架构，小范围社区传播 | 法律义务未履行；但执法抓手（商店下架、域名拦截）均不适用 | ✅ **当前阶段推荐** |
| **B. 建最小服务端以完成备案** | 买一台最低配云服务器 + 域名（约 ¥100-300/年），只放静态页与更新清单 | 打破"无自有服务端"原则；引入运维负担与成本 | ⚠️ 规模扩大后考虑 |
| **C. 用 GitHub Pages 替代，不备案** | 更新源与官网都在 GitHub Pages | GitHub 是境外平台，不在境内备案体系内；但**因此也无法完成备案** | ✅ 与 A 配套 |

**诚实结论**：

> 对于一个**学生、开源、非盈利、无服务端、不收集数据、不收费**的小范围项目，
> 备案的**实际风险敞口很低**，但**法律义务在形式上并未消失**。
>
> 这不构成法律意见。**如果项目做大（比如用户数上万），或开始接受捐赠之外的任何收费，
> 必须重新评估并补办备案。** 不要抱着"永远不备案"的侥幸做长期规划。

### 1.4 学生身份的三个特殊注意点

| 注意点 | 说明 | 应对 |
|--------|------|------|
| **毕业后项目交接** | 学生项目最大的风险是"人走了项目死了" | ① 许可证选 GPL-3.0（保证 fork 后仍开源）② 文档写全 ③ 尽早招募共同维护者 ④ 仓库所有权不要绑死在个人学号邮箱 |
| **学校政策** | 部分学校对学生在校成果的知识产权有规定 | ⚠️ **建议确认学校的学生成果归属政策**，尤其是使用学校资源/设备/导师指导的情况 |
| **不以盈利为目的的边界** | "非盈利"不等于"零成本" | ✅ 接受捐赠（GitHub Sponsors）通常不改变非盈利定性，但**若捐赠与功能挂钩则可能被认定为经营**。建议纯自愿、无对价 |

### 1.5 非盈利带来的产品影响

| 影响 | 说明 |
|------|------|
| ✅ 不需要商业化功能 | 无会员、无内购、无广告、无增长黑客，功能设计更纯粹 |
| ✅ 社区信任更容易建立 | "学生开源非盈利"本身就是最强的信任背书 |
| ❌ 无预算做推广 | 只能靠口碑，必须把产品体验和文档做到极致 |
| ❌ 无预算做机型适配 | 只能靠社区用户帮忙测试（需要设计好反馈渠道） |
| ❌ 无预算做安全审计 | 只能靠开源审查 + 自动化扫描（需在 CI 里做足） |

---

## 2. 虚拟屏幕：技术可行性与完整方案

> 这是本版最重要的章节。结论可能和你的预期不一样，请仔细看。

### 2.1 平台硬约束：AOSP 的明确规定

**AOSP 官方文档《activity 启动政策》原文**：

> 在 Android 10 中，为了防止恶意应用通过从其创建的虚拟屏幕的表面读取用户敏感信息来盗用相关信息，
> **应用只能在其创建的虚拟屏幕上启动其自己的 Activity。**

**具体规则表**：

| 调用方 | 能否在虚拟屏/副屏启动 Activity |
|--------|-------------------------------|
| 普通第三方应用（默认） | ❌ 只能在自己创建的虚拟屏上启动**自己的** Activity |
| 有 `INTERNAL_SYSTEM_WINDOW` 权限的系统组件 | ✅ 可在任何屏幕启动 |
| 有 `ACTIVITY_EMBEDDING` 权限的调用方 | ⚠️ 仅限目标应用声明了 `FLAG_ALLOW_EMBEDDED` |
| 专有屏幕（proprietary display） | ⚠️ 仅屏幕所有者或已在该屏的 Activity |

**检查 API**：`ActivityManager#isActivityStartAllowedOnDisplay(context, displayId, intent)`
**实现位置**：`ActivityStackSupervisor#isCallerAllowedToLaunchOnDisplay()`

### 2.2 那为什么还有工具能做到？

因为 **shell 权限走的是另一条路**。

- `am` 命令由 shell 执行时，调用身份是 **shell UID (2000)**，与普通应用不同
- `am start --display <id>` 是 `ActivityManagerShellCommand` 官方支持的参数
- Shizuku 提供的正是 **shell UID 的能力**

**经验证据**：开源项目 SecondaryScreen（副屏模拟器）的文档明确说明——
"可通过 shell 命令提权，使用 `am start` 命令启动 secondActivity，并通过 `--display` 参数指定到副屏上"。

> ⚠️ **但这需要实测确认**。不同厂商 ROM 对 shell 的权限授予策略可能不同，
> 且 Android 大版本可能继续收紧。**这是 EX-16 要回答的核心问题。**

### 2.3 方案矩阵：四条路，一条主路

| 方案 | 机制 | 能否跑第三方 App | 依赖 | 用户干扰 | 评价 |
|:---:|------|:---:|------|----------|------|
| **A. Overlay Display（副屏）** | `settings put global overlay_display_devices` + `am start --display` | ✅ 需 Shizuku | WRITE_SECURE_SETTINGS + shell | 低（副屏窗可缩放移动） | ⭐ **主路** |
| **B. Freeform 小窗** | `enable_freeform_support` + `am start --windowingMode 5` | ✅ 需 Shizuku | WRITE_SECURE_SETTINGS + shell | 中（小窗占屏） | 备选 |
| **C. MediaProjection 虚拟屏** | `MediaProjection.createVirtualDisplay()` | ❌ 只能镜像，不能跑别人 App | MediaProjection 授权 | 无 | 仅用于**读取** |
| **D. 全屏接管** | 直接在默认显示上操作 | ✅ 零依赖 | 无障碍 | 高（占满屏幕） | 兜底 |

### 2.4 方案 A 详解：Overlay Display

#### 创建副屏

```bash
# 创建一块 1080x1920、320dpi 的副屏
settings put global overlay_display_devices "1080x1920/320"

# 创建多块（最多 6 块）；带 secure 参数时无法截屏/录屏
settings put global overlay_display_devices "1080x1920/320,secure;1080x1920/320,secure"

# 删除
settings put global overlay_display_devices "null"
```

**权限**：`WRITE_SECURE_SETTINGS`（signature|privileged，但**可通过 ADB/Shizuku 授予**：`pm grant <pkg> android.permission.WRITE_SECURE_SETTINGS`）

**副屏特征**（实测输出）：
```
DisplayInfo{"叠加视图 #1", displayId 5, FLAG_PRESENTATION, FLAG_TRUSTED,
            real 1920 x 1080, type OVERLAY, layerStack 5}
```

#### 在副屏上启动目标 App

```bash
am start --display 5 -n com.tencent.mm/.ui.LauncherUI
```

或从代码：
```kotlin
// 方式一：走 Shizuku 执行 shell 命令（推荐，不受 setLaunchDisplayId 限制）
Shizuku.newProcess(arrayOf("am", "start", "--display", "5", "-n", "$pkg/$activity"), null, null)

// 方式二：ActivityOptions（⚠️ 受 Android 10 安全限制，第三方应用大概率被拒）
val opts = ActivityOptions.makeBasic().apply { launchDisplayId = 5 }
startActivity(intent, opts.toBundle())
```

> **实现建议**：优先用方式一（Shizuku shell），方式二仅作为探测手段。

#### 输入注入到副屏

```bash
input -d 5 tap 400 640
input -d 5 swipe 400 800 400 200 300
input -d 5 text "hello"
```

**关键优势**：`-d <displayId>` 让输入只作用于副屏，**不干扰用户在主屏的触摸操作**。这正是需求的核心。

#### 读取副屏 UI 树

```kotlin
// Android 13+ (API 33)：支持跨显示获取窗口
val nodes = if (Build.VERSION.SDK_INT >= 33) {
    accessibilityService.getWindowsOnAllDisplays()
        .filterKeys { it == targetDisplayId }
        .values.flatten()
        .mapNotNull { it.root }
} else {
    // Android 12 及以下：用 windows 过滤
    accessibilityService.windows
        .filter { it.displayId == targetDisplayId }
        .mapNotNull { it.root }
}
```

> ⚠️ **注意**：`getWindowsOnAllDisplays()` 需要 `flagRetrieveInteractiveWindows` 标志
> （已在 v2.0 的无障碍服务配置中声明）。

#### 截取副屏画面

```bash
screencap -d 5 -p /sdcard/agent_display5.png
```

**只能走 shell**（Shizuku）。MediaProjection **无法截取副屏**。

> ⚠️ 若副屏创建时带了 `secure` 参数，则无法截屏。**所以不要带 `secure`**。

#### 方案 A 的已知限制（必须写进产品）

| 限制 | 说明 | 应对 |
|------|------|------|
| **副屏窗口在主屏上可见** | 叠加在主屏上，可缩放、可移动，**但不可隐藏** | ① 缩到最小尺寸并移到屏幕角落 ② 提供"最小化预览"开关 ③ 明确告知用户这是系统行为，非本应用所致 |
| **最多 6 块副屏** | 系统限制 | 单实例使用即可，无需多块 |
| **部分 App 不支持多显示** | 未声明 `resizeableActivity="true"` 的 App 可能无法在副屏正常渲染，或黑屏 | 检测失败后自动降级到方案 B/D |
| **金融类 App 可能拒绝运行** | 检测到非默认显示可能主动退出 | 命中即停止并提示用户（与敏感页面策略一致） |
| **依赖 Shizuku** | 用户需安装 Shizuku 并通过无线调试激活；**重启后需重新激活** | ① 提供完整的激活引导 ② 检测失效后降级 ③ 主推无障碍通道作为日常，虚拟屏作为增强 |
| **副屏音频/通知归属** | 副屏上 App 的音频仍从设备输出，通知正常 | 一般无问题，但需实测 |
| **强依赖 WRITE_SECURE_SETTINGS** | 需 Shizuku 授权 | 同上 |

### 2.5 方案 B 详解：Freeform 小窗（备选）

```bash
# 开启 freeform 支持
settings put global enable_freeform_support 1
settings put global force_resizable_activities 1

# 以小窗模式启动，并指定边界
am start --windowingMode 5 --bounds 100,200,900,1400 -n com.tencent.mm/.ui.LauncherUI
```

**优点**：不需要创建虚拟屏，小窗可移动、可最小化，用户体验比副屏窗口好
**缺点**：仍占用主屏空间；用户可能误触；部分 App 不支持 freeform

### 2.6 推荐的降级链（必须实现）

```
启动 Agent 任务
      ↓
① 虚拟屏模式（方案 A）
   ├─ Shizuku 可用？ ── 否 ──┐
   ├─ 创建副屏成功？ ── 否 ──┤
   ├─ App 启动到副屏成功？ ── 否 ──┤
   └─ 成功 → 在副屏执行，主屏完全不受影响 ✅
                                  ↓
② 小窗模式（方案 B）
   ├─ freeform 可用？ ── 否 ──┐
   └─ 成功 → 小窗执行，主屏可正常使用 ⚠️
                              ↓
③ 全屏接管模式（方案 D）
   └─ 在默认显示上执行，明确告知用户"我正在操作你的屏幕"
      · 提供中止按钮
      · 用户可随时接管
                              ↓
④ 手动引导模式
   └─ 只给出操作步骤，用户自己点
```

**产品要求**：
- 当前处于哪一档，必须在 UI 上**明确显示**（如"当前模式：虚拟屏 / 小窗 / 全屏"）
- 用户可在设置中**强制指定**模式（如"永远不要占用我的屏幕"→ 只用虚拟屏或手动引导）
- 降级时给出**原因说明**，而不是默默降级

### 2.7 关键结论

> **"调用虚拟屏幕，不影响用户正常使用"这个需求——技术上可达，但有三个前提：**
>
> 1. **必须有 Shizuku**（无障碍权限做不到，这是 Android 10 起的硬限制）
> 2. **副屏窗口在主屏上仍可见**（系统行为，可缩放移动但不可隐藏）
> 3. **不是所有 App 都能在副屏跑**（未声明 resizeableActivity 的会失败）
>
> 如果这三条中有任何一条无法接受，那么"虚拟屏"应降级为**可选增强**，
> 而非产品的默认形态。**建议产品默认走小窗模式，虚拟屏作为"高级用户可选"。**

---

## 3. 执行层重构：以显示为第一维度

### 3.1 接口变更（重要）

v2.0 的 `ActionExecutor` 没有"显示"概念。**虚拟屏引入后，所有动作都必须带 displayId。**

```kotlin
// 新增：执行目标
sealed interface ExecutionTarget {
    /** 默认显示（物理屏幕）—— 会干扰用户 */
    data object PrimaryDisplay : ExecutionTarget

    /** 虚拟屏 —— 不干扰用户 */
    data class VirtualDisplay(val displayId: Int) : ExecutionTarget

    /** 小窗（freeform） */
    data object FreeformWindow : ExecutionTarget
}

// ActionExecutor 接口变更
interface ActionExecutor {
    val channel: ExecutorChannel

    /**
     * 新增：该通道支持哪些执行目标。
     * 无障碍通道只能操作默认显示；虚拟屏只有 Shizuku 通道支持。
     */
    fun supportedTargets(): Set<Class<out ExecutionTarget>>

    suspend fun perform(action: UiAction, target: ExecutionTarget): ActionResult
}
```

### 3.2 通道与执行目标的对应关系（关键矩阵）

| 执行目标 | 无障碍 | Shizuku | IME | 悬浮窗引导 |
|----------|:---:|:---:|:---:|:---:|
| **默认显示（主屏）** | ✅ | ✅ | ✅ | ✅ |
| **虚拟屏（副屏）** | ❌ 只能读，不能注入 | ✅ **唯一可行** | ❌ | ❌ |
| **小窗（freeform）** | ✅（在默认显示上） | ✅ | ✅ | ✅ |

**这张表是整个架构的核心约束**：

- 读虚拟屏：无障碍可以（`getWindowsOnAllDisplays`）
- 操作虚拟屏：**只有 Shizuku**
- 截虚拟屏：**只有 Shizuku**（`screencap -d`）

> **结论：虚拟屏模式是"Shizuku 独占"的能力。没有 Shizuku 就没有虚拟屏。**

### 3.3 新增模块：`:display`

```kotlin
/**
 * 虚拟显示管理器。
 *
 * 职责：创建/销毁虚拟屏、在虚拟屏上启动 App、判断虚拟屏可用性。
 *
 * ⚠️ 本模块**必须依赖 Shizuku**。无 Shizuku 时所有方法返回 Unavailable，
 *    由上层降级到小窗或全屏模式。
 */
interface VirtualDisplayManager {
    /** 当前是否可用（Shizuku 已激活 + WRITE_SECURE_SETTINGS 已授予） */
    fun availability(): DisplayAvailability

    /** 创建（或复用）虚拟屏，返回 displayId */
    suspend fun ensureDisplay(config: DisplayConfig): DisplayResult

    /** 在虚拟屏上启动目标 App */
    suspend fun launchOnDisplay(packageName: String, displayId: Int): LaunchResult

    /** 检查某 App 能否在虚拟屏运行（预检，避免黑屏） */
    suspend fun canRunOnDisplay(packageName: String): Boolean

    /** 释放虚拟屏并清理副屏上的 App */
    suspend fun release()

    /** 观察虚拟屏上的 App 状态 */
    fun observeRunningApps(displayId: Int): Flow<Set<String>>
}

data class DisplayConfig(
    val width: Int = 1080,
    val height: Int = 1920,
    val densityDpi: Int = 320,
    /** ⚠️ 不要设 true，否则无法截屏 */
    val secure: Boolean = false,
)

sealed interface DisplayResult {
    data class Created(val displayId: Int) : DisplayResult
    data object Unavailable : DisplayResult
    data class Failed(val reason: String) : DisplayResult
}
```

### 3.4 坐标映射（虚拟屏预览交互）

如果要在主屏上提供一个可交互的副屏预览窗，需要坐标映射：

```kotlin
/**
 * FIT_CENTER 缩放 + letterbox 偏移。
 * 用户在预览窗上点击 → 换算成副屏坐标 → 通过 input -d 注入。
 */
fun mapToVirtualDisplay(
    touchX: Float, touchY: Float,
    viewWidth: Int, viewHeight: Int,
    virtualWidth: Int, virtualHeight: Int,
): Point? {
    val scale = minOf(viewWidth.toFloat() / virtualWidth, viewHeight.toFloat() / virtualHeight)
    val renderedW = virtualWidth * scale
    val renderedH = virtualHeight * scale
    val offsetX = (viewWidth - renderedW) / 2f
    val offsetY = (viewHeight - renderedH) / 2f

    val contentX = touchX - offsetX
    val contentY = touchY - offsetY
    // 点在黑边上，忽略
    if (contentX < 0 || contentY < 0 || contentX > renderedW || contentY > renderedH) return null

    return Point(
        x = (contentX / renderedW * virtualWidth).toInt().coerceIn(0, virtualWidth - 1),
        y = (contentY / renderedH * virtualHeight).toInt().coerceIn(0, virtualHeight - 1),
    )
}
```

### 3.5 生命周期与资源释放（易错点）

**必须严格按顺序释放**，否则会残留副屏或黑屏：

```
1. 隐藏副屏预览悬浮窗
2. force-stop 副屏上运行的 App（am force-stop）
3. 释放 VirtualDisplay
4. 关闭 ImageReader
5. 删除 overlay_display_devices 设置
```

**常见坑**：
- `ImageReader` 的 `maxImages` 必须设小（2），且必须**及时 `acquireLatestImage().close()`**
- 不消费帧会导致 buffer 满，副屏 App 停止渲染（表现为"卡死"）
- 应用崩溃/被杀时副屏不会自动清理，**下次启动必须做孤儿副屏检测**

---

## 4. GitHub 仓库推送能力设计

> 需求："项目达到一定规模后，我会提供一个可推送到指定 GitHub 仓库的密钥。"

### 4.1 安全红线（最重要的一条）

> ### ⚠️ 绝对不要把 GitHub token 硬编码进 APK
>
> APK 是可以被任何人反编译的。一旦 token 内置：
> - 任何人都能提取它，往你的仓库里灌垃圾、开几百个 issue、甚至删除内容
> - GitHub 会自动检测到泄露的 token 并吊销，但你的仓库可能已经被搞了
> - 而且**你无法追责**——因为从 GitHub 的角度看，这就是"你的应用在正常操作"
>
> **这是新手最容易犯的致命错误。** 见 4.2 的正确做法。

### 4.2 三种正确做法（按推荐度排序）

#### 方案 1：GitHub OAuth Device Flow ★推荐

```
用户在 App 内点「连接到 GitHub」
      ↓
App 向 GitHub 请求 device_code + user_code
      ↓
App 展示 8 位码，引导用户打开 github.com/login/device 输入
      ↓
用户在自己账号下授权
      ↓
App 轮询换取 access_token（存在用户设备上）
      ↓
此后以**用户自己的身份**提交内容
```

**优点**：
- ✅ **token 属于用户，不属于你**——用户可随时在 GitHub 设置里撤销
- ✅ 不需要 client secret（public client 模式），**没有可泄露的长期凭据**
- ✅ Client ID 是公开的，硬编码在 APK 里也没关系
- ✅ 责任清晰：用户授权了什么，用户自己知道

**实现要点**：
- 需注册一个 GitHub OAuth App（免费，5 分钟）
- 只需 `public_repo` 或更细粒度的 scope
- token 用与 API Key 相同的 Keystore 加密存储（复用 `CryptoManager`）
- 提供"断开连接"按钮，同时引导用户去 GitHub 撤销

#### 方案 2：BYO Token（用户自备令牌）

与 BYOK 理念完全一致，非常适合本项目：

```
App 引导用户去 GitHub Settings → Developer settings
      → Fine-grained tokens → 生成一个只对指定仓库有 issues:write 权限的 token
      → 粘贴到 App 里
```

**优点**：
- ✅ 实现最简单（就是一个输入框 + 一次验证）
- ✅ 权限可以精确到"只对某一个仓库、只有 issues 写权限"
- ✅ 与 BYOK 的产品理念一致，用户容易理解

**缺点**：
- ⚠️ 用户操作门槛略高（要会生成 PAT）
- ⚠️ Fine-grained token 有过期时间，需要提醒用户续期

#### 方案 3：不推送，生成内容让用户手动提交

```
App 生成一份格式化的报告（Markdown）
      ↓
用户点「复制」或「分享」
      ↓
用户自己粘贴到 GitHub Issue 页面
```

**优点**：零安全风险、零实现成本
**缺点**：体验差，转化率低

**建议**：**方案 1 为主，方案 2 为备，方案 3 兜底。**

### 4.3 推送能力的应用场景

| 场景 | 推送内容 | 目标 | 需要的权限 |
|------|----------|------|-----------|
| **崩溃/错误报告** | 脱敏后的堆栈 + 设备信息 + 复现步骤 | 指定仓库的 Issue | `issues:write` |
| **用户写的规则提交** | 用户创作的选择器规则（L1 规则包） | 官方规则仓库的 PR | `contents:write` + `pull_requests:write` |
| **插件提交** | 完整的插件包 + manifest | 插件索引仓库的 PR | 同上 |
| **误报反馈** | "我的规则没生效/误触了" + 快照 | 对应插件的 Issue | `issues:write` |
| **匿名遥测**（可选，默认关） | 功能使用频次统计 | 仓库中的 JSON 文件 | `contents:write` |
| **机型适配反馈** | "这个 ROM 上权限引导路径不一样" | 适配仓库的 Issue | `issues:write` |

> **最有价值的两个场景**：**用户规则提交（PR）** 和 **机型适配反馈**。
> 前者直接喂养插件生态，后者解决"没有预算买测试机"的死结——**让社区帮你测**。

### 4.4 架构要求

```kotlin
/**
 * GitHub 上报通道。
 *
 * ⚠️ 三条硬性约束：
 * 1. **凭据必须运行时注入**，绝不硬编码。未配置时所有方法直接返回 NotConfigured
 * 2. **默认关闭**。用户必须显式授权后才启用
 * 3. **所有内容必须经用户预览确认后才能发送**
 */
interface GitHubReporter {
    /** 当前连接状态 */
    fun connectionState(): ConnectionState

    /** 通过 Device Flow 发起连接 */
    suspend fun startDeviceFlow(): DeviceFlowSession

    /** 用 BYO token 连接 */
    suspend fun connectWithToken(token: String, repo: String): ConnectionResult

    /** 断开连接（同时提示用户去 GitHub 撤销） */
    suspend fun disconnect()

    /** 提交报告（内容必须先经用户确认） */
    suspend fun submitReport(report: ReportPayload): SubmitResult

    /** 提交规则/插件为 PR */
    suspend fun submitContribution(contribution: ContributionPayload): SubmitResult
}

sealed interface ConnectionState {
    /** 未配置 —— 所有上报功能静默关闭，不打扰用户 */
    data object NotConfigured : ConnectionState
    data class Connected(val login: String, val repo: String) : ConnectionState
    data class Expired(val reason: String) : ConnectionState
    data class Failed(val reason: String) : ConnectionState
}
```

**关键设计**：
1. **未配置时完全静默**——不弹窗、不提示、不报错。功能就是"不存在"
2. **凭据用 `CryptoManager` 加密存储**（与 API Key 同一套机制，复用现有代码）
3. **提交前必须展示预览**——用户看到要发什么，点确认才发
4. **提交内容必须经 `LogSanitizer` 脱敏**——防止把 Key、手机号、截图内容带出去
5. **频次限制**——同一类报告每天最多 N 次，防止刷屏和触发 GitHub 限流
6. **失败静默**——上报失败不影响主功能，最多记本地日志

### 4.5 阶段性启用

| 阶段 | 状态 | 说明 |
|------|------|------|
| M0-M2 | **不实现** | 没有 token，也不需要 |
| M3 | 实现框架 | 接口、加密存储、Device Flow 打通，但默认关闭 |
| M4+ | 正式启用 | 用户规模起来后，引导用户连接 GitHub |
| 未来 | 提供仓库 | **届时把仓库地址配置化**（可通过订阅源或本地配置下发，不硬编码） |

> **重要**：仓库地址也应该是**可配置的**，而不是硬编码。
> 这样将来换仓库（比如从个人仓库迁到组织仓库）不需要发新版。

---

## 5. 开源项目治理

### 5.1 许可证选择（有坑）

| 依赖 | 许可证 | 传染性 | 对本项目的约束 |
|------|--------|:---:|----------------|
| **GKD**（若参考其规则格式） | **GPL-3.0** | ⚠️ 强 copyleft | **若复用其代码，本项目必须也是 GPL-3.0** |
| Shizuku | Apache-2.0 | 无 | 可自由使用 |
| AndroidX / Compose | Apache-2.0 | 无 | 可自由使用 |
| SQLCipher | BSD-style | 无 | 可自由使用 |
| QuickJS | MIT | 无 | 可自由使用 |
| ML Kit | Google 条款 | ⚠️ 特殊 | 需查看具体条款，非标准开源 |

**建议**：

| 选项 | 适用场景 | 评价 |
|------|----------|------|
| **GPL-3.0** | 想防止闭源套壳、想复用 GPL 生态（如 GKD 的规则格式与实现） | ⭐ **推荐**，与社区分发 + 非盈利定位最匹配 |
| Apache-2.0 | 想最大化扩散、允许闭源使用 | 不利于防止套壳 |
| MIT | 最宽松 | 同上 |

> ⚠️ **重要提醒**：**如果参考了 GKD 的规则格式设计（这几乎是必然的），
> 要区分"参考设计思想"（不受版权保护）与"复制代码"（受版权保护）**。
> 抄代码 → 必须 GPL-3.0；只借鉴格式与理念 → 不受约束。
> **建议：直接选 GPL-3.0，省去所有纠结，也与社区精神一致。**

### 5.2 仓库结构建议

```
pocketagent/                     主仓库（GPL-3.0）
├── app/                         本体
├── plugin-api/                  插件契约（插件作者需要）
├── docs/                        文档与官网源文件
├── rules/                       内置示例规则（非官方规则集）
└── .github/
    ├── ISSUE_TEMPLATE/
    │   ├── bug_report.yml
    │   ├── rom_compat.yml       ← 机型适配反馈模板（社区帮你测）
    │   └── rule_request.yml
    └── workflows/
        ├── build.yml
        ├── security-scan.yml    ← Key 泄漏正则扫描
        └── release.yml

pocketagent-plugins/             官方插件索引（可独立仓库）
├── index.json                   订阅源清单
└── plugins/                     官方维护的插件

pocketagent-plugin-template/     插件模板（fork 即用）
└── ...

community-rules/                 社区规则集（第三方维护，用 GitHub Topic 聚合）
```

**GitHub Topic 约定**：`pocketagent-plugin`、`pocketagent-rules`
→ 本体内置的"社区源列表"通过这个 topic 聚合，无需自建服务端。

### 5.3 贡献流程

```
贡献者 fork → 修改 → 提交 PR
      ↓
CI 自动检查
├── 规则语法校验
├── 选择器合法性检查
├── 是否包含敏感页面操作（自动拒绝）
└── 是否申请了禁止的能力
      ↓
维护者 review（重点看安全边界）
      ↓
合并 → 自动更新订阅源 index.json
```

**贡献者守则（必须写进 CONTRIBUTING.md）**：
- ❌ 禁止提交针对支付、转账、密码、验证码页面的规则
- ❌ 禁止提交抢红包、刷单、薅羊毛类规则
- ❌ 禁止提交游戏辅助类规则
- ❌ 禁止在规则中申请 `payment.*` / `key.*` 等禁止能力
- ✅ 提交前必须用快照工具验证过
- ✅ 必须说明规则的适用场景与已知误触风险

### 5.4 可持续性设计（学生项目的关键）

| 风险 | 应对 |
|------|------|
| 作者毕业/工作后无暇维护 | ① 尽早找共同维护者 ② 文档写全，降低接手成本 ③ 用 Issue/PR 模板降低沟通成本 |
| 作者账号丢失 | ① 仓库可转移到组织 ② 保留多个管理员 ③ 签名密钥要有安全备份（丢了就无法发更新！） |
| 资金枯竭 | ① 零服务端架构天然零成本 ② GitHub Actions 免费额度足够 ③ 可选接受捐赠 |
| 社区分裂 | ① 许可证保证 fork 后仍开源 ② 不要搞"官方唯一"心态 ③ 拥抱衍生项目 |

> ⚠️ **签名密钥备份是生死问题**：APK 签名密钥一旦丢失，你就**再也无法给已安装的用户推送更新**
> （因为签名不一致，覆盖安装会失败）。必须：
> ① 离线备份到多个位置 ② 写进交接文档 ③ 公布公钥指纹以便将来验证

---

## 6. 修订后的模块划分

### 6.1 v3.0 新增模块

| 模块 | 职责 | 优先级 |
|------|------|:---:|
| `:display` | **虚拟屏管理**（创建/销毁/启动 App/可用性检测） | P0 |
| `:display:preview` | 副屏预览悬浮窗 + 坐标映射 | P1 |
| `:github` | GitHub 连接（Device Flow / BYO Token）、上报通道 | P2（M3 实现） |
| `:contribute` | 规则/插件贡献流程（生成 PR、内容预览） | P2 |

### 6.2 更新后的整体结构

```
┌───────────────────────────────────────────────────────────┐
│                       app (UI 壳)                          │
│  对话 │ Key 管理 │ 任务 │ 插件页 │ 设置 │ 权限引导 │ 贡献页  │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                    plugin 层（v2.0）                        │
│  api │ runtime │ rules │ script │ store │ devtools         │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                  display 层（v3.0 新增）                    │
│  VirtualDisplayManager │ 副屏预览 │ 坐标映射 │ 生命周期      │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                     Domain Layer                           │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                       Data Layer                           │
│  Room │ OkHttp │ Keystore │ :update │ :channel │ :github    │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                   Platform Layer                           │
│  无障碍 │ **Shizuku（核心）** │ MediaProjection │ 悬浮窗     │
│  通知 │ IME │ 前台服务                                      │
└───────────────────────────────────────────────────────────┘
```

### 6.3 关键依赖约束（更新）

- `:display` **必须依赖 `:action:shizuku`**，不得依赖无障碍（无障碍无法操作虚拟屏）
- `:agent` 不感知 display 细节，只通过 `ExecutionTarget` 抽象选择执行目标
- `:github` 的凭据存储**必须复用 `:core:crypto`**，不得自建加密逻辑
- `:github` 未配置时，所有调用方必须能正常降级（不得抛异常）

---

## 7. 修订后的分阶段计划

### 7.1 M0 新增实验（最高优先级）

#### EX-16 虚拟屏可行性验证 ★★★ 决定性问题

| 项 | 内容 |
|----|------|
| **假设** | Shizuku 的 shell 权限可以通过 `am start --display` 把第三方 App 启动到 overlay display 上 |
| **方法** | ① 用 Shizuku 授予 `WRITE_SECURE_SETTINGS` ② 创建 overlay display ③ 记录 displayId ④ 用 Shizuku 执行 `am start --display <id> -n 微信/美团` ⑤ 观察是否成功启动、是否黑屏 ⑥ 用 `input -d <id> tap` 验证输入注入 ⑦ 用 `screencap -d <id>` 验证截图 ⑧ 用无障碍 `getWindowsOnAllDisplays()` 验证 UI 树读取 |
| **通过标准** | 至少 3 个主流 App 能在副屏正常启动并可被操作 |
| **失败预案** | **若失败，虚拟屏需求不成立**。产品降级为"小窗模式为主 + 全屏接管兜底"，需重新评估"不影响用户使用"这一需求能否满足 |
| **产出** | 《虚拟屏可行性报告》+ 可复用代码 |

**必须测试的 App 类型**（覆盖不同 resizeableActivity 情况）：
- 声明了 `resizeableActivity="true"` 的（大多数现代 App）
- 未声明的（老 App、部分工具类）
- 明确拒绝多显示的（金融类、安全类）
- 游戏类

#### EX-17 虚拟屏下的完整链路验证

| 项 | 内容 |
|----|------|
| **假设** | 在副屏上能完成"读屏 → 决策 → 操作 → 校验"完整闭环 |
| **方法** | 在副屏上跑通"打开设置 → 进入 WLAN 页面"，全程不触碰主屏 |
| **通过标准** | 任务成功，且**主屏全程可正常使用**（可用另一个手指操作主屏验证） |
| **产出** | 虚拟屏模式的可运行 Demo |

#### EX-18 副屏窗口的干扰度评估

| 项 | 内容 |
|----|------|
| **假设** | 副屏窗口缩到最小并移到角落后，对用户主屏使用的干扰可接受 |
| **方法** | ① 测量副屏窗口的最小尺寸 ② 测试能否移到屏幕边缘之外 ③ 邀请 3-5 人实测，评估主观干扰度 |
| **通过标准** | 大多数用户认为"基本无感" |
| **失败预案** | 若干扰明显，则虚拟屏只作为"高级用户可选"，默认用全屏接管模式 |

### 7.2 修订后的路线图

```
M0 技术验证（4周，新增虚拟屏实验）
  └─ EX-16 虚拟屏可行性 ★决定性问题
  └─ EX-13 侧载受限设置 ★决定性问题
  └─ EX-14 K60 2K 屏成本
  ↓
M1 MVP + 插件框架（8-10周）
  ↓
M2 任务引擎 + 脚本插件 + 虚拟屏集成（8-10周）
  ↓
M3 体验 + 开发工具 + GitHub 通道（6周）
  ↓
M4 开源治理 + 发布（4周）
  └─ 许可证、签名公示、误报申诉、文档、官网
  ↓
M5 生态运营（持续）
```

> **注意**：M0 从 3 周延长到 4 周，因为虚拟屏验证的不确定性很高。

### 7.3 M0 的两个"决定性问题"

| 实验 | 如果通过 | 如果失败 |
|------|----------|----------|
| **EX-16 虚拟屏** | 可以实现"不影响用户使用"的核心卖点 | 产品降级为小窗/全屏模式，需求需重新协商 |
| **EX-13 受限设置** | 侧载场景下无障碍可用 | 整个产品形态在 Android 13+ 侧载场景不成立 |

**这两个实验必须最先做，且必须在真机上做。** 其他所有工作都建立在它们成立的前提上。

---

## 8. 待确认决策点（v3.0）

v2.0 的 D11-D20 中，以下因新信息已可确定：

| # | 决策点 | v3.0 结论 | 理由 |
|---|--------|-----------|------|
| D11 | 开源许可证 | ✅ **GPL-3.0** | 非盈利 + 防套壳 + 可能参考 GPL 生态 |
| D17 | 是否收费 | ✅ **不收费，可接受自愿捐赠** | 学生非盈利定位；避免经营性认定 |
| D20 | App 备案 | ✅ **暂不办，接受风险** | 无服务端则无法备案（结构性死结）；小范围传播敞口低 |

**新增决策点**：

| # | 决策点 | 选项 | 建议 |
|---|--------|------|------|
| **D21** | 虚拟屏在产品的地位 | 默认形态 / 高级可选 / 不做 | **高级可选**。默认走全屏接管或小窗，虚拟屏作为"Shizuku 用户专享" |
| **D22** | 是否需要 Shizuku | 必须 / 可选 / 不用 | **可选但强烈推荐**。文档中明确"装了 Shizuku 才有完整体验" |
| **D23** | GitHub 连接方式 | Device Flow / BYO Token / 手动提交 | **Device Flow 为主 + BYO Token 为备 + 手动提交兜底** |
| **D24** | 上报功能的默认状态 | 开 / 关 | **默认关**，用户显式授权后启用 |
| **D25** | 仓库地址是否硬编码 | 硬编码 / 可配置 | **可配置**（通过订阅源或本地配置），便于将来迁移仓库 |
| **D26** | 是否内置社区规则集 | 内置 / 不内置 | **不内置，只提供订阅源列表且默认关闭**（避免"官方推荐了坏规则"的责任） |
| **D27** | 项目主仓库位置 | 个人仓库 / 组织仓库 | **尽早建 GitHub Organization**（便于交接、多人维护、避免绑死个人账号） |

---

## 附：本版核心结论速览

> **1. 身份变化让合规大幅简化，但备案有个结构性死结：没有服务器就没法备案。**
>    当前阶段建议接受风险，规模扩大后重新评估。
>
> **2. 虚拟屏需求技术上可达，但只有 Shizuku 一条路。**
>    Android 10 起，普通应用不能把别人的 App 启动到虚拟屏上——这是 AOSP 的硬规定。
>    而且副屏窗口在主屏上**可见**（可缩放移动但不可隐藏），"完全无感"做不到。
>    **建议把虚拟屏定位为"高级用户可选增强"，而非默认形态。**
>
> **3. GitHub 密钥绝不能硬编码进 APK。**
>    用 OAuth Device Flow，让 token 属于用户而不是你。
>    这既解决了安全问题，又和 BYOK 的产品理念一脉相承。
>
> **4. 两个决定性问题必须在 M0 最先回答：虚拟屏（EX-16）和侧载受限设置（EX-13）。**
>    它们不成立，产品形态就要重写。别先写代码。

---

*文档结束。建议下一步：确认 D21-D27，然后立刻开始 EX-16 与 EX-13 的真机验证。*
